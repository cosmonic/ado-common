# TODO: Set up for all OS types similar to what is found here https://github.com/crate-ci/azure-pipelines/blob/master/install-rust.yml
steps:
  - bash: sudo snap install wash --devmode --edge
    displayName: "Install wash"
